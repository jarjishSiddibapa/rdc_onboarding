# Environment
.env
*.env

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/

# Virtual environments
venv/
.venv/
env/

# Flask / app
instance/
uploads/

# Truein dry-run output files (contain real API tokens — never commit)
truein_dryruns/

# Docs/collections that embed live API credentials verbatim (confirmed
# 2026-09-15 — all three contain the same Truein subscription/access keys
# that were also hardcoded in app/integrations/truein.py before that date).
# Real values now live in .env only.
Truein_API_Developer_Reference.html
TRUEIN_API_GUIDE.html
Truein_API.postman_collection.json

# Raw employee/staffing data exports (real PII — names, employee codes,
# departments, ...) pulled from ZingHR/Truein/DVT during earlier one-off
# analysis work. Never belongs in git history, private repo or not.
/*.xlsx

# Generated graph artifact — large, fully regenerable via `graphify update .`
graphify-out/

# Testing
.coverage
htmlcov/
.pytest_cache/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
